import { StyleSheet } from 'react-native';

const textStyles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#000',
  },
  body: {
    fontSize: 17,
    color: '#000',
  },
  button: {
    fontSize: 17,
    fontWeight: 'bold',
    color: '#fff',
  },
  buttonDisabled: {
    fontSize: 17,
    fontWeight: 'bold',
    color: '#fff',
    opacity: 0.5,
  },
});

export default textStyles;
