import { StyleSheet } from 'react-native';

const textStyles = StyleSheet.create({
  title: {
    fontSize: 16,
    fontWeight: 'normal',
    color: '#000',
  },
  body: {
    fontSize: 16,
    fontWeight: 'normal',
    color: '#000',
  },
  button: {
    fontSize: 16,
    fontWeight: 'normal',
    color: '#fff',
  },
  buttonDisabled: {
    fontSize: 16,
    fontWeight: 'normal',
    color: '#fff',
    opacity: 0.5,
  },
});

export default textStyles;
